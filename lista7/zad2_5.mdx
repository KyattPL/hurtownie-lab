SELECT {[DIM CUSTOMER].[Country Region Code].CHILDREN} ON COLUMNS,
	{ORDER(FILTER(
		{[DIM PRODUCT].[Category Name].CHILDREN*[DIM PRODUCT].[Sub Category Name].CHILDREN},
		[Measures].[Product ID Distinct Count] >= 10),
		[Measures].[Order Qty],
		DESC
	)} ON ROWS
	FROM [AW_2019_DSV];